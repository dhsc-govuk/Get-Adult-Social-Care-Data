services:
    app:
        container_name: gascd_app
        image: gascd_frontend_image
        build:
            context: .
        ports:
            - 80:80
        env_file:
            - .env
        environment:
            - PORT=80